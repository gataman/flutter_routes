import 'package:flutter/material.dart';

class AuthMainView extends StatelessWidget {
  const AuthMainView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('AuthMainView');
  }
}
